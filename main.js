import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return <h1>Welcome to Service in Demand Frontend!</h1>;
};

ReactDOM.render(<App />, document.getElementById('app'));
